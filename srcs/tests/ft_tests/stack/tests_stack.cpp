/* ************************************************************************** */
/*                                                                            */
/*                                                        :::      ::::::::   */
/*   main.cpp                                           :+:      :+:    :+:   */
/*                                                    +:+ +:+         +:+     */
/*   By: dodjian <dovdjianpro@gmail.com>            +#+  +:+       +#+        */
/*                                                +#+#+#+#+#+   +#+           */
/*   Created: 2021/12/24 20:28:18 by dodjian           #+#    #+#             */
/*   Updated: 2021/12/26 17:25:07by dodjian          ###   ########.fr       */
/*                                                                            */
/* ************************************************************************** */

#include "vector.hpp"

/* void	test_constructor_and_iterator()
{
	std::cout << BRED << "Create obj with all constructor !" << END << std::endl << std::endl;

	ft::stack<int> first;				// empty ft::stack of ints
	ft::stack<int> second(4,100);		// four ints with value 100
	ft::stack<int> third(second.begin(), second.end());  // iterating through second
	ft::stack<int> fourth(third); // copy
	std::cout << BYELLOW << "The contents of first are:" << BBLUE;
	for (ft::stack<int>::iterator it = first.begin(); it != first.end(); ++it)
		std::cout << ' ' << *it;
	std::cout << std::endl;
	std::cout << BYELLOW << "The contents of second(4, 100) are:" << BBLUE;
	for (ft::stack<int>::iterator it = second.begin(); it != second.end(); ++it)
		std::cout << ' ' << *it;
	std::cout << std::endl;
	std::cout << BYELLOW << "The contents of third(second.begin, second.end) are:" << BBLUE;
	for (ft::stack<int>::iterator it = third.begin(); it != third.end(); ++it)
		std::cout << ' ' << *it;
	std::cout << std::endl;
	std::cout << BYELLOW << "The contents of fourth(third) are:" << BBLUE;
	for (ft::stack<int>::iterator it = fourth.begin(); it != fourth.end(); ++it)
		std::cout << ' ' << *it;
	std::cout << std::endl;

	// the iterator constructor can also be used to construct from arrays:
	int myints[] = {16,2,77,29};
	ft::stack<int> fifth(myints, myints + sizeof(myints) / sizeof(int));

	std::cout << BYELLOW << "The contents of fifth are:" << BBLUE;
	for (ft::stack<int>::iterator it = fifth.begin(); it != fifth.end(); ++it)
		std::cout << ' ' << *it;
	std::cout << std::endl;
	std::cout << BYELLOW << "The contents of fifth in reverse are:" << BBLUE;
	for (ft::stack<int>::reverse_iterator it = fifth.rbegin(); it != fifth.rend(); ++it)
		std::cout << ' ' << *it;
	std::cout << std::endl << std::endl << END;
}

void	test_relational_operators()
{
	std::cout << BRED << "Test relational operator !" << END << std::endl << std::endl;
	//ft::stack v;
	ft::stack<int> foo (3,100);	// three ints with a value of 100
	ft::stack<int> bar (2,200);	// two ints with a value of 200

	if (foo == bar)
		std::cout << "foo and bar are equal" << std::endl;
	if (foo != bar)
		std::cout << "foo and bar are not equal" << std::endl;
	if (foo < bar)
		std::cout << "foo is less than bar" << std::endl;
	if (foo > bar)
		std::cout << "foo is greater than bar" << std::endl;
	if (foo <= bar)
		std::cout << "foo is less than or equal to bar" << std::endl;
	if (foo >= bar)
		std::cout << "foo is greater than or equal to bar" << std::endl;
}

void	test_operator()
{
	std::cout << BRED << "Test all operator !" << END << std::endl << std::endl;
	test_relational_operators();
}
*/

void	tests_stack()
{

}
